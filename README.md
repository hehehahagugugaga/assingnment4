# assingnment4

task1 : the first task reads the file named sample.txt and returns the data stored in it. If the file does not exist, it will return a proper error message
task2 : this file writes the user input in the file and also writes additional data from the user and after that, it gives the total content in the output.txt file
